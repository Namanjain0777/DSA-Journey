```
🟢 EASY LEVEL (Foundations – MUST DO)


Find the maximum and minimum element in an array

Reverse an array in-place

Count occurrences of a given element

Check if an array is sorted or not

Find the sum of all elements

Find the index of a target element using Linear Search

Find second largest element in an array

Print elements at even indices only

Move all zeros to the end

Swap alternate elements
```
ArrayList
```
Insert elements into an ArrayList and print them

Remove all even numbers from an ArrayList

Find maximum element in an ArrayList

Check if an element exists using contains()

Reverse an ArrayList
```
🟡 MEDIUM LEVEL (Core DSA – VERY IMPORTANT)

Linear & Binary Search
```
Implement Linear Search (return index or -1)

Implement Binary Search (iterative)

Implement Binary Search (recursive)

Find first occurrence of an element in sorted array

Find last occurrence of an element in sorted array

Count number of occurrences of a target

Find the floor of a number in a sorted array

Find the ceiling of a number in a sorted array

Find the smallest element greater than target
```
Order-Agnostic Binary Search
```
Implement Order-Agnostic Binary Search

Search an element in a descending sorted array

Detect whether array is ascending or descending
```
2D Arrays
```
Search an element in a 2D matrix (brute force)

Print row-wise sum of a matrix

Print column-wise sum of a matrix

Find the maximum element in each row

Count total elements greater than a given number
```
🔵 INTERVIEW LEVEL (🔥 MUST PRACTICE)

Binary Search Variations
```
Find peak element in an array

Find minimum element in a sorted rotated array

Search an element in a sorted rotated array

Find the number of times array is rotated

Find first and last position of an element (LeetCode classic)
```

2D Matrix (VERY IMPORTANT 🔥)
```
Search in a row-wise and column-wise sorted matrix (staircase search)

Return position [row, col] of target or [-1, -1]

Count number of elements less than or equal to target in sorted matrix

Find the row with maximum number of 1s

Search in a fully sorted matrix using binary search

Search in matrix treating it as a flattened sorted array
```
Array + Binary Search Combo
```
Find the k-th smallest element in a sorted matrix

Find the median of a row-wise sorted matrix

Find the smallest element common in all rows

Find element closest to target
```
🔴 INTERVIEW + THINKING LEVEL (TOP COMPANIES 🔥)
```
Infinite sorted array → find position of target

Find element in a nearly sorted array

Find the missing number using binary search

Search in a matrix with unknown dimensions

Minimize the maximum difference between elements

Allocate minimum number of pages (Binary Search on Answer)

Aggressive cows problem (Binary Search on Answer)
```

Easy → 1–15 (confidence)

Medium → 16–32 (core strength)

Interview → 33–47 (placements)

Hard → 48–54 (top-tier interviews)