# 🟣 NEXT QUESTIONS (AFTER WHAT YOU’VE DONE)

## 🔥 ARRAY CORE – NEXT STEP (THINKING HEAVY)

1. Find the **third largest element** without sorting
2. Find **all leaders** in an array
3. Find **equilibrium index** of an array
4. Maximum sum **subarray of size k** (sliding window intro)
5. Maximum **product subarray**
6. Find **majority element** (> n/2 times)
7. Find **elements appearing more than n/3 times**
8. Find **missing and repeating number**
9. Rearrange array **alternating positive & negative**
10. Rotate array by **k steps** (left & right)

---

## 🔥 PREFIX / RANGE BASED (INTERVIEW FAVORITE)

11. Find **range sum queries** efficiently
12. Subarray with **sum = k**
13. Count subarrays with **sum = 0**
14. Longest subarray with **equal 0s and 1s**
15. Longest subarray with **sum divisible by k**
16. Find number of subarrays with **odd sum**
17. Find number of subarrays with **XOR = k**

👉 *These make you stand out.*

---

## 🔥 ADVANCED BINARY SEARCH (REAL INTERVIEW LEVEL)

18. Binary Search on **Answer pattern – identify it**
19. Split array into **k subarrays** minimizing max sum
20. Find **minimum speed** to reach destination on time
21. Minimize **days to make bouquets**
22. Find **minimum capacity to ship packages**
23. Find **minimum time to complete jobs**
24. Koko eating bananas
25. Painter’s partition problem

---

## 🔥 ROTATED + DUPLICATES (VERY IMPORTANT)

26. Search in **rotated sorted array with duplicates**
27. Find **minimum in rotated array with duplicates**
28. Count rotations when duplicates exist
29. Find **pivot index** properly
30. Check if array is **rotated and sorted**

---

## 🔥 2D MATRIX – NEXT LEVEL 🔥

31. Find **median** in row-wise sorted matrix (optimized)
32. Count **negative numbers** in sorted matrix
33. Find **maximum sum submatrix**
34. Rotate matrix **90 degrees in-place**
35. Transpose matrix **without extra space**
36. Set matrix zeroes (constant space)
37. Spiral traversal of matrix
38. Boundary traversal of matrix

---

## 🔥 ARRAY + MATH + BS MIX (TOP COMPANY)

39. Find **smallest divisor** given threshold
40. Minimize **maximum distance between gas stations**
41. Find **k closest elements** to target
42. Find **k weakest rows** in matrix
43. Find **minimum absolute difference** pair
44. Find **maximum distance j - i such that arr[j] ≥ arr[i]**

---

## 🔥 THINKING / TRAP QUESTIONS (PLACEMENTS)

45. Can binary search work on **unsorted array**? (when & how)
46. Why binary search fails sometimes even on sorted arrays
47. Binary search vs two pointers — when to choose what
48. Why binary search on answer works
49. Find time complexity of **nested binary search problems**

---

## 🔥 FINAL BOSS (DO THESE = CONFIDENT AF)

50. Median of **two sorted arrays**
51. K-th element of **two sorted arrays**
52. Allocate books with **maximum students constraint**
53. Aggressive cows – explain logic clearly
54. Explain **binary search invariants**

