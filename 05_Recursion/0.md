# Recursion
Function Call Itself Directly or inDirectly until a base condition is met 

Recursive Call Should move towards the best case

Use Function call stack (LIFO)

Call Stack -> when a Function call itself , each call is stored in stack memory 

Probelm -> Stack Overflow -> If it goes to infinity / Too Many recursive Call

# RECURSION TREE

Useful for finding time Complexity 

Used in Optimazation(like memorization)

Space Complexity  = O(Recursion Depth)

Recursive function consume extra memory dure to call stack

Deep Recursion is risky -> Prefer iteration / tail recursuion when possible

## Base Condition

Multiple Base case possible

It should ensure termination

## Recursion 
Parameterisead -> Parameter carry state between calls

Functional -> Return results to previous Calls

## Multiple recursion call

Each call spawn tro more recusrive call

Complexity grows exponentialy (O(2^N))

## Tail Recursion 

When the recursive call is the leate statemnet in function 

It save Stack space 

Compiler can optimize it to reuse Stack Frames

### Indirectly Recursion 
function call another function 

### Tree Recursion 
 Occur when each function call branches into two or more recursive calls

# Tips
1. Str.CharAt()
2. Str.lenght()
3. Str.replaceAll("[^a-zA-Z0-9]"," ") -> It remove spaces , symbol , non-alphanumaric

